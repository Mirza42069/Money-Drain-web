const authConfig = {
    providers: [
        {
            domain: "https://renewing-condor-90.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
